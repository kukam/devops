Welcome to devops container!

See <https://wiki.debian.org/>
See <https://packages.debian.org/index/>
See <https://hub.docker.com/_/debian>
See <https://hub.docker.com/r/kukam/devops>
See <https://github.com/kukam/devops/>


Example commands:
=================

$ rabbitmqadmin -H <SERVER_IP> -u <USER_NAME> -p <PASSWORD> list vhosts
$ mysql -u <USER_NAME> -p -h <SERVER_IP> <DATABASE_NAME>
$ psql -U <USER_NAME> -h <SERVER_IP> -d <DATABASE_NAME>
$ redis-cli -h <SERVER_IP> -p <SERVER_PORT> -a <PASSWORD>
$ and more: [ansible, terraform, tofu, helm, hemlfile, docker, kubectl, calicoctl, az, rabbitmqadmin, kcat, etc...]
